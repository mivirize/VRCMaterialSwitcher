# VRC Material Switcher マニュアル

VRChat アバターのマテリアル切替メニューを Modular Avatar (MA) ベースで生成するエディタ拡張の詳細マニュアルです。
クイックスタートは [README.md](README.md) を参照してください。

## 目次

1. [仕組み — 何が生成されるか](#1-仕組み--何が生成されるか)
2. [インストールとアップグレード](#2-インストールとアップグレード)
3. [自動セットアップの手順](#3-自動セットアップの手順)
4. [スキャンの仕組みと命名規則](#4-スキャンの仕組みと命名規則)
5. [マテリアルグループの編集](#5-マテリアルグループの編集)
6. [レンダラーマッピング（適用先の指定）](#6-レンダラーマッピング適用先の指定)
7. [メニュー設定とパラメータ](#7-メニュー設定とパラメータ)
8. [テクスチャ容量の見積りと削減](#8-テクスチャ容量の見積りと削減)
9. [手動セットアップ（ツールを使わない場合）](#9-手動セットアップツールを使わない場合)
10. [既存セットアップの管理と削除](#10-既存セットアップの管理と削除)
11. [ユーティリティ](#11-ユーティリティ)
12. [トラブルシューティング](#12-トラブルシューティング)

---

## 1. 仕組み — 何が生成されるか

「セットアップ実行」を押すと、アバター直下に次の GameObject 階層が作られます。
**アバター本体・FX レイヤー・既存メニューアセットは一切変更しません**（非破壊）。

```
アバター
└─ VRCMaterialSwitcherRoot
   │   MA Menu Item (SubMenu "衣装カラー")
   │   MA Menu Installer          ← ビルド時にメニューへ合流させる
   │   MA Parameters              ← パラメータ宣言 (Int / saved / synced / 既定値)
   │
   ├─ Group_Onepiece
   │   │   MA Menu Item (SubMenu "Onepiece")
   │   ├─ Black   … MA Menu Item (Toggle, MatSwitch_Onepiece = 0) + MA Material Setter
   │   ├─ White   … MA Menu Item (Toggle, MatSwitch_Onepiece = 1) + MA Material Setter
   │   └─ Red     … MA Menu Item (Toggle, MatSwitch_Onepiece = 2) + MA Material Setter
   └─ Group_Ribbon
       └─ …
```

- 実際のマテリアル差し替えは MA の **Material Setter** が行います（ビルド時にアニメーションへ変換される）
- パラメータは **1 グループ = Int 1 個**（`MatSwitch_<グループ名>`）
- この階層を削除すれば元通りになります（＝アンインストールも非破壊）

## 2. インストールとアップグレード

**前提**: Unity 2022.3.x / VRChat SDK Avatars 3.x / Modular Avatar 1.9+

1. [Releases](../../releases) から `VRCMaterialSwitcher.unitypackage` をダウンロード
2. Unity プロジェクトにインポート（`Assets/Editor/VRCMaterialSwitcher/` に入ります）

**v1.1 以前からのアップグレード**: 旧版は不要になったファイルを含むため、
`Assets/Editor/VRCMaterialSwitcher` フォルダを削除してから新版をインポートしてください。
設定ファイル（`ProjectSettings/VRCMaterialSwitcherSettings.json`）は引き継がれます。

> v1.2 から、エディタ起動時に自動実行されていた残骸クリーンアップは**廃止**しました。
> クリーンアップは手動メニューから、対象を確認したうえで実行する方式になっています（[§11](#11-ユーティリティ)）。

## 3. 自動セットアップの手順

### Step 1: アバターの指定

シーン上のアバター（VRCAvatarDescriptor 付き）を**アバター**欄にドラッグします。
「シーンからアバターを自動検出」ボタンでも設定できます。
衣装は事前にアバターへ着せておいてください（Modular Avatar 衣装ならシーン上で親子付け済みの状態）。

### Step 2: スキャン

**スキャンフォルダ**に衣装のマテリアルが入っているフォルダ（例: `Assets/<衣装名>/Materials`）を
ドラッグして「🔍 スキャン実行」を押します。サブフォルダも自動で走査されます。

スキャン結果として:
- **マテリアルグループ** — パーツごとの色バリエーション一覧
- **グループ化できなかったマテリアル** — 単独マテリアルなど。手動でグループ化できます（[§5](#5-マテリアルグループの編集)）
- **シェーダー混在の警告** — 1 グループに別種のマテリアルが混ざった可能性

### Step 3: グループの確認・調整

- グループ名・バリエーション名は Expression Menu の表示名になります。自由に編集できます
- メニューに入れない色は**チェックを外す**
- **★** ボタンでデフォルトの色を指定（初期値はアバターが現在着ている色になります）

### Step 4: レンダラーマッピングの確認

スキャン時に、各グループのマテリアルがアバターのどのメッシュ・スロットで使われているかを
自動検出します。「対象レンダラー」セクションで確認し、誤りがあれば手動修正します（[§6](#6-レンダラーマッピング適用先の指定)）。

### Step 5: セットアップ実行

「👁 プレビュー」で生成内容を事前確認できます。「✓ セットアップ実行」で生成します。
何度実行しても結果は同じです（再実行すると生成物を作り直します）。

### Step 6: アップロードと動作確認

VRChat SDK のビルドパネルからアップロードし、ゲーム内の Expression Menu →「衣装カラー」で
切替を確認します。エディタ上で確認したい場合は Gesture Manager や Av3Emulator が使えます。

## 4. スキャンの仕組みと命名規則

BOOTH 等で配布される衣装のフォルダ構成・命名は制作者ごとにバラバラです。
本ツールは次の 3 つの手がかりを**優先順**に使って「パーツ（切替対象）」と「色（バリエーション）」を推定します。

### 検出スキーマ

| 優先 | 手がかり | 判定内容 | 例 |
|---|---|---|---|
| 1 | **UV トークン** | 名前に `UV1` `uv_2` 等があれば、同じ UV 番号 = 同じパーツとしてグループ化 | `UV1 Check/xxx.mat` と `UV1 Plain/yyy.mat` → 同じグループ |
| 2 | **パーツ名フォルダ** | フォルダ名が色名でも汎用名（Materials 等）でもなければ、フォルダ = 1 パーツ | `Obi/black.mat`, `Obi/red.mat` → グループ「Obi」 |
| 3 | **ファイル名トークン** | 色フォルダ・汎用フォルダ内では、ファイル名から色トークンを除いた共通部分 = パーツ | `shirt_black.mat`, `shirt_white.mat` → グループ「shirt」 |

バリエーション名は「グループ内で変化している軸」（ファイル名の差分 or フォルダ名の差分）から自動生成します。
ネスト構造（`Black/Check/xxx.mat` のような色×柄）は両方の名前を保持します。

### うまく検出できる命名の例

```
Materials/shirt_black.mat, shirt_white.mat          → shirt グループ (black / white)
Obi/Kenjo.mat, Obi/Shima.mat                        → Obi グループ (Kenjo / Shima)
Black/Onepiece.mat, White/Onepiece.mat              → Onepiece グループ (Black / White)
UV1 Check/A.mat, UV1 Plain/B.mat                    → UV1 グループ (Check / Plain)
```

### 苦手なケースと対処

| ケース | 起きること | 対処 |
|---|---|---|
| 辞書にない色名（作者独自名・日本語名） | 色ではなくパーツ名と誤判定され、グループが分裂する | キーワード辞書に追加（下記） |
| 1 フォルダに複数パーツの色違いが混在 | 1 グループに統合される | UI でグループを分割（[§5](#5-マテリアルグループの編集)） |
| 名前に規則性がない | グループ化されず「未分類」に残る | 手動でグループ作成（[§5](#5-マテリアルグループの編集)） |

### 色キーワード辞書の拡張

内蔵辞書（英語色名・柄名・ローマ字色名など約 90 語）にない色名は、プロジェクト直下の
`ProjectSettings/VRCMaterialSwitcherKeywords.json` を作成して追加できます:

```json
{
  "colorKeywords": ["sakura", "yamabuki", "kohaku"],
  "genericFolderNames": ["textures", "MAT"]
}
```

- `colorKeywords` — 「これはバリエーション（色・柄）を表す語」として扱う
- `genericFolderNames` — 「これはパーツ名ではない汎用フォルダ名」として扱う

ファイルを置いた後、再度スキャンすると反映されます（Unity 再起動は不要ですが、
一度読み込んだ後に JSON を書き換えた場合はスクリプトのリロードが必要です）。

## 5. マテリアルグループの編集

自動検出の結果は UI 上で自由に編集できます。**スキャンし直すと手動編集は失われる**ため、
編集はスキャン結果を確定させてから行ってください。

| 操作 | 方法 |
|---|---|
| グループを新規作成 | 「＋ 新規グループ作成」→ 名前を付けてバリエーションを追加 |
| グループを削除 | グループ右上の ✕ |
| グループをメニューから除外 | グループ名左のチェックを外す（削除せず無効化） |
| バリエーションを追加 | グループ内の「＋ バリエーション追加」→ マテリアルをドラッグ |
| バリエーションを外す | 行末の － |
| 表示名を変える | 名前欄を直接編集（Expression Menu の表示名になります） |
| デフォルト色を変える | 行頭の ★ |
| グループを分割したい | 新規グループを作り、片方のバリエーションを移し替える |

「グループ化できなかったマテリアル」に載った単独マテリアルも、新規グループ＋バリエーション追加で
メニューに組み込めます（例: 眼鏡の ON 用マテリアルと OFF 用マテリアルを 1 グループにする等）。

## 6. レンダラーマッピング（適用先の指定）

各グループには「どのレンダラーのどのマテリアルスロットを差し替えるか」の情報（適用先）が必要です。

- **自動マッピング** — スキャン時に自動実行されます。「自動マッピング」ボタンで再実行できます。
  グループ内のマテリアルをアバター上で検索し、使われている (レンダラー, スロット) を**すべて**登録します。
  浴衣の上下のように同じマテリアルを複数メッシュで使う衣装にも対応します
- **手動マッピング** — 「＋ 対象レンダラー追加」でレンダラーをドラッグし、スロットをプルダウンで選択。
  アバター配下以外のレンダラーは登録できません
- マッピングが 1 件もないグループはセットアップ時にスキップされます（UI に表示されます）

**自動マッピングの前提**: アバターが現在そのグループのマテリアルの**いずれか**を着ている必要があります。
どの色も適用されていないパーツ（別衣装のマテリアル等）は検出できないため、手動で追加してください。

## 7. メニュー設定とパラメータ

| 設定 | 意味 | 既定 |
|---|---|---|
| メニュー名 | Expression Menu に追加されるサブメニューの名前 | 衣装カラー |
| パラメータを保存 (saved) | ワールド移動・再起動後も選んだ色を維持する | ON |
| パラメータを同期 (synced) | 他プレイヤーからも色が変わって見える | ON |

- パラメータは 1 グループ = Int 1 個（synced の場合 **8 bit** 消費）。
  アバターの同期パラメータ上限は 256 bit なので、グループが多い場合は注意してください。
  現在の消費量はウィンドウ下部のコスト表示で確認できます
- パラメータ名はグループ名から自動生成されます（例: `MatSwitch_Onepiece`）。
  日本語や記号は `_` に変換され、重複時は連番が付きます

## 8. テクスチャ容量の見積りと削減

セットアップセクションの上に、切替対象テクスチャとアバター全体のテクスチャ容量の試算が表示されます。
VRChat の Poor 判定や VRAM 逼迫が気になる場合:

- 「↓ 切替色のみ N 以下に」— 切替対象のテクスチャだけ最大解像度を下げる
- 「↓ アバター全体を N 以下に」— アバターが参照する全テクスチャを下げる

いずれも Texture Importer の Max Size を変更します（元画像は劣化しません。
Max Size を戻せば元に戻ります）。

## 9. 手動セットアップ（ツールを使わない場合）

本ツールが生成するのは標準的な Modular Avatar コンポーネントだけなので、
同じものを手で組むこともできます。ツールの生成結果を微調整したい場合や、
仕組みを理解したい場合は以下の手順を参考にしてください。

### 手順（1 パーツ・3 色の例）

1. アバター直下に空の GameObject を作成（例: `MyMaterialMenu`）
2. これに以下のコンポーネントを追加:
   - **MA Menu Installer** — （設定不要。インストール先を変えたい場合のみ指定）
   - **MA Menu Item** — Type: `SubMenu`, Source: `Children` に設定
   - **MA Parameters** — 「パラメータを追加」で以下を宣言:
     - 名前: `MyColor`（任意）/ 型: `Int` / Saved: ✓ / Synced: ✓ / 既定値: 0
3. その子に色の数だけ GameObject を作成（例: `Black`, `White`, `Red`）
4. 各色オブジェクトに:
   - **MA Menu Item** — Type: `Toggle`, パラメータ: `MyColor`, 値: `0`（色ごとに 0, 1, 2…）
   - **MA Material Setter** — 「対象を追加」でレンダラーを指定し、
     対象マテリアルスロットに差し替え先マテリアルを設定
5. 複数メッシュを連動させたい場合は、同じ Material Setter に対象を追加していく
6. デフォルトにしたい色の MA Menu Item で `Default` にチェック

これはまさに本ツールが「セットアップ実行」で自動生成している構造です（[§1](#1-仕組み--何が生成されるか)）。

### ツール UI だけで手動構成する場合

スキャンを使わず「＋ 新規グループ作成」からグループを作り、バリエーションと適用先レンダラーを
手動で登録して「セットアップ実行」する方法もあります。フォルダ構成が特殊でスキャンが
役に立たない衣装ではこちらが早いです。

## 10. 既存セットアップの管理と削除

ウィンドウ最下部の「既存セットアップ」セクションに、アバター上の生成済みグループが表示されます。

- **グループ単位で削除** — 各行の「削除」
- **全削除（アンインストール）** — 「全セットアップを削除」。`VRCMaterialSwitcherRoot` ごと削除します。
  生成物はこの階層に閉じているため、これで完全に元通りになります

`Assets/VRCMaterialSwitcher_Generated/` フォルダ（メニュー/パラメータアセットが未設定だった
アバター用に自動作成される受け皿）は、不要になったら手動で削除して構いません。

## 11. ユーティリティ

`Tools > VRC Material Switcher` 配下:

| メニュー | 用途 |
|---|---|
| Fix Streaming Mip Maps (Project-wide) | プロジェクト全体で、Mipmap 有効なのに Streaming Mip Maps が無効なテクスチャを一括修正（VRChat アップロード時のバリデーションエラー対策） |
| Fix Streaming Mip Maps (Scene Avatars) | シーン上のアバターが参照するテクスチャのみ修正 |
| 残骸パラメータのクリーンアップ (選択アバター) | v1.0 系（FX 直接書き込み方式）が残した `MatSwitch_*` パラメータ・FX レイヤーを、**削除対象を確認したうえで**選択中のアバターから削除 |
| Export UnityPackage (Dev) | 開発者用。配布パッケージには含まれません |

いずれも実行前に確認ダイアログが出ます。

## 12. トラブルシューティング

### スキャンしてもグループが出ない / 分け方がおかしい
[§4](#4-スキャンの仕組みと命名規則) の検出スキーマと苦手ケースを確認してください。
未分類リストに残ったものは手動グループ化（[§5](#5-マテリアルグループの編集)）で対応できます。

### 「対象レンダラーなし」と表示される
アバターがそのグループのマテリアルをどれも着ていない状態です。いずれかの色を一度適用するか、
手動で対象レンダラーを追加してください（[§6](#6-レンダラーマッピング適用先の指定)）。

### ゲーム内でメニューは出るが色が変わらない
- Modular Avatar が最新か確認（Material Setter は MA 1.9 以降）
- 対象レンダラー・スロットのマッピングが正しいか確認
- ローカルでは Gesture Manager / Av3Emulator でのみ動作確認できます（エディタの再生だけでは動きません）

### アップロード時に "mipmapped textures without Streaming Mip Maps" エラー
`Tools > VRC Material Switcher > Fix Streaming Mip Maps (Project-wide)` を実行してください。

### パラメータ上限 (256 bit) を超えた
- 不要なグループのチェックを外す（1 グループ = 8 bit）
- 「パラメータを同期」を OFF にする（他人からは見えなくなりますが 0 bit になります）
- 使っていない他ギミックのパラメータを整理する

### Unity 再起動後に設定が消えた / マテリアルが None になった
v1.1 以前の既知バグです。v1.2 で修正されました（GUID による参照復元）。
v1.2 以降でも発生する場合は Issue で報告してください。
