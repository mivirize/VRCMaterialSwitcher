# Changelog

## v1.2.3 (2026-08-12)

### メニュー

- 大見出しを `MIVI` から **`MIVI Works`** に変更
  (`MIVI Works > VRC Material Switcher > ...`)

### レイアウト

- **ウィンドウを狭めたときに内容が右側で見切れる経路を解消**
  - 横スクロールバーを出さず、内容を常にウィンドウ幅に収めるようにした
  - ラベル幅をウィンドウ幅に追従させた (従来は 150px 固定で、狭いウィンドウでは入力欄を圧迫していた)
- **バリエーション行の既定マーカー (●/○) が潰れて読めなくなる問題を修正**
  グループ枠の padding に加えて字下げが入り、固定幅の列を内側から削っていた。
  字下げを廃し、表示名欄はウィンドウ幅に追従するようにした
- `EditorGUI.indentLevel` の戻し漏れが起きうる箇所を解消
- スキャン実行・クリア・グループ追加・バリエーション追加の各操作後に描画フレームを分離
  (操作でセクション数が変わる際のレイアウト不整合を防止)

### ドキュメント

- スクリーンショット付きの配布用マニュアルを追加

## v1.2.2 (2026-08-12)

### メニュー再編

- メニューを `Tools > VRC Material Switcher` から **`MIVI > VRC Material Switcher`** へ移動
  （MIVI ブランドの大見出し配下に整理）。項目: ウィンドウを開く / Fix Streaming Mip Maps
  (Project-wide) / 残骸パラメータのクリーンアップ
- `Fix Streaming Mip Maps (Scene Avatars)` を削除（Project-wide と役割が重複し使われていなかったため）

## v1.2.1 (2026-08-11)

### 自動マッピングの全面改修 (最重要)

v1.2.0 までの自動マッピングは「スキャンしたマテリアルアセットがアバター上でそのまま使われている」
ことを暗黙の前提にしており、参照が一致しない衣装では **0 件マッチ**になっていた。
実際の衣装は prefab の既定マテリアルと色バリエーションが別アセットであることが多く、
色フォルダをスキャンした時点でこの前提が崩れる。

- **参照が一致しない場合も適用先を推定できるようにした。**
  同名マテリアル / 共通テクスチャの共有 / 部位キー（色名を除いた共通トークン）/ メインテクスチャ /
  レンダラー名 / シェーダーを重み付けし、確度が十分で他候補と差があるものだけ自動採用する
- **決められない場合は候補をスコアと根拠つきで提示**し、ユーザーが選べるようにした
  （誤マッチで無関係な部位まで切り替わる事故を避けるため、迷ったら自動採用しない）
- 「候補なし」の場合は**アバターの全スロットから手動選択**できるようにした
- グループごとに **確定 / 推定 / 要確認 / 候補なし** と判断根拠を表示
- 自動で決められなかったグループの**既存設定を上書きしなくなった**（手動設定が消えない）
- 同名オブジェクト / 名前に `/` を含むオブジェクトを検出して警告

### Modular Avatar への受け渡しの修正

- **Material Setter に直接参照を渡すようにした** (`AvatarObjectReference.Set`)。
  従来はパス文字列のみを設定していたため、同名オブジェクトがあると別のメッシュに適用される、
  アバタールート直下のレンダラーは空パスとなり MA 側で解決されない、という問題があった
- 適用先が解決できない設定（リネーム・階層変更・別アバターの設定が残っている等）を
  セットアップ前に検出してエラーにするようにした（壊れた生成物を作らない）
- 適用先が未設定のグループがある場合もセットアップ前にエラーにする（何もしないメニューを作らない）
- 同名オブジェクトの適用先が重複判定で脱落する問題を修正

### 検証

Komano で自動マッピングの回帰テスト 6 件を追加し、計 18 検査が全 PASS
（未割当バリエーションの推定 2 パターン / 誤マッチ拒否 / 手動設定の保護 / 直接参照の生成 / 壊れた適用先の拒否）。

## v1.2.0 (2026-08-10)

### 安全性 (重要)

- **エディタ起動時の自動クリーンアップを廃止。** 旧版はエディタ起動のたびに、シーン内の全アバターから
  `MatSwitch_*` パラメータ・FX レイヤーを無確認で削除していた（他ツールが同じ接頭辞を使っていた場合、
  その資産も破壊されうる）。v1.2 では手動メニューからのみ実行され、削除対象を列挙して確認したうえで
  選択中のアバター 1 体だけを対象にする
- Fix Streaming Mip Maps (Scene Avatars): mipmap 無効テクスチャの誤書き換えと null 参照クラッシュを修正。
  実行前の確認ダイアログを追加。メニュー名を実挙動に合わせ "Active Avatar" → "Scene Avatars" に変更

### 修正

- **Unity 再起動でマテリアル参照が消える問題を修正** (Asset GUID による参照復元)
- 再起動後にスキャンフォルダ欄が復元されない問題を修正
- グループ/バリエーション削除が即時保存されない問題を修正
- アバターを切り替えてもテクスチャ容量表示が更新されない問題を修正
- 有効バリエーションが 2 未満のグループが空メニューとして生成される問題を修正
- デフォルト色をメニューから除外した場合に既定値が不定になる問題を修正
- グループ名が衝突するとパラメータを共有して連動してしまう問題を修正 (一意名を自動割当)
- グループ名/バリエーション名の `/` が階層パスとして解釈され再実行・削除が壊れる問題を修正
- 再実行時にリネーム・無効化した旧グループの生成物が残る問題を修正 (ルート配下を毎回再構築)
- MA Parameters を明示生成するように変更 (saved / synced / 既定値が既存パラメータ定義に左右されず確実に適用される)
- セットアップ前にマテリアル未設定バリエーションを検証してエラー表示

### 改善 (スキャン)

- 色キーワード辞書をユーザー拡張可能に (`ProjectSettings/VRCMaterialSwitcherKeywords.json`)
- グループ化できなかったマテリアルを UI に表示 (手動グループ化の手がかり)
- シェーダーが混在するグループに警告を表示 (別パーツ混入の検出)
- デフォルト色の初期値を「アバターが現在着ている色」に変更 (旧: アルファベット順先頭)
- 共通プレフィックス検出をスペース・ハイフン区切りの命名にも対応 (旧: `_` のみ)

### 構造

- `MaterialSwitcherWindow.cs` (1,386 行) を役割別に分割
- 開発用 `PackageExporter` を配布パッケージから除外し、ハードコードされた出力先を修正
- 設定保存をデバウンス化 (毎キー入力でのディスク書き込みを解消)
- リポジトリに .meta ファイルを追加 (GUID 安定化)。unitypackage は Releases からの配布に一本化

### ドキュメント

- README 全面書き直し
- MANUAL.md 新設 (命名規則・手動セットアップ・トラブルシューティング)

## v1.1

- 初回リリース (自動スキャン / 自動マッピング / MA セットアップ / Streaming Mip Maps 修正)
